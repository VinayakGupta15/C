#include<stdio.h>

int main(){
    // int i = 34;
    // int *ptr = &i;
    // printf("The value of ptr is %u\n", ptr);
    // // ptr = ptr - 1;
    // ptr = ptr + 1;
    // //  ptr++;
    // //   ptr++;

//     char i = 34;
//    char *ptr = &i;
//     printf("The value of ptr is %u\n", ptr);
//     // ptr = ptr - 1;
//     ptr = ptr + 1;
//     //  ptr++;
//     //   ptr++;

    float f = 34;
    float *ptr = &f;
    printf("The value of ptr is %u\n", ptr);
    // ptr = ptr - 1;
    ptr = ptr + 2;
    
    //  ptr++;
    //   ptr++;
    return 0;
}